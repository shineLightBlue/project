import { Route, Redirect, RouteProps, useLocation } from 'react-router-dom'

const Video = (props: RouteProps) => {
    // console.log(props)
    return (
        <div>Video</div>
    )
}
export default Video